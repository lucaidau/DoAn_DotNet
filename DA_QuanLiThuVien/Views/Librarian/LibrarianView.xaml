<UserControl x:Class="DA_QuanLiThuVien.Views.Librarian.LibrarianView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DA_QuanLiThuVien.Views.Librarian"
             xmlns:vmFeatures="clr-namespace:DA_QuanLiThuVien.ViewModels.Main.Librarian.Features"
             xmlns:viewsFeatures="clr-namespace:DA_QuanLiThuVien.Views.Librarian.Features"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Menu Thủ Thư -->
        <Border Grid.Column="0" Background="#F5F9FF" BorderBrush="#D5DFEA" BorderThickness="0,0,1,0">
            <StackPanel Margin="16,24,16,16">
                <TextBlock Text="Chức Năng" FontSize="16" FontWeight="Bold" Foreground="#0B2545" Margin="0,0,0,16"/>
                
                <ItemsControl ItemsSource="{Binding DanhSachChucNang}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding TenChucNang}" 
                                    Command="{Binding Command}" 
                                    Margin="0,0,0,8" 
                                    Padding="12,10" 
                                    Background="#E8F1FF" 
                                    Foreground="#0B2545"
                                    BorderThickness="0"
                                    HorizontalAlignment="Stretch"
                                    FontSize="12">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            CornerRadius="8" 
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#D0E5FF"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Nội dung chính -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Tiêu đề chức năng -->
            <Border Grid.Row="0" Background="White" Padding="24,16" BorderBrush="#D5DFEA" BorderThickness="0,0,0,1">
                <TextBlock Text="{Binding TieuDeChucNang}" FontSize="18" FontWeight="Bold" Foreground="#0B2545"/>
            </Border>

            <!-- Content Area - Hiển thị Feature ViewModel -->
            <ContentControl Grid.Row="1" Content="{Binding ViewModelHienTai}" Padding="24,24">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type vmFeatures:QuanLySachViewModel}">
                        <viewsFeatures:QuanLySachView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:QuanLyTacGiaViewModel}">
                        <viewsFeatures:QuanLyTacGiaView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:QuanLyBanSaoSachViewModel}">
                        <viewsFeatures:QuanLyBanSaoSachView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:NhapSachViewModel}">
                        <viewsFeatures:NhapSachView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:MuonTraViewModel}">
                        <viewsFeatures:MuonTraView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:QuanLyDocGiaViewModel}">
                        <viewsFeatures:QuanLyDocGiaView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:BaoCaoViewModel}">
                        <viewsFeatures:BaoCaoView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:ThongKeNangCaoViewModel}">
                        <viewsFeatures:ThongKeNangCaoView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:CaiDatViewModel}">
                        <viewsFeatures:CaiDatView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vmFeatures:QuanLyTaiKhoanDocGiaViewModel}">
                        <viewsFeatures:QuanLyTaiKhoanDocGiaView/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>
    </Grid>
</UserControl>
