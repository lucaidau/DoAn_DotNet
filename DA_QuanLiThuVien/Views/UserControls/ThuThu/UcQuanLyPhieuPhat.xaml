<UserControl x:Class="DA_QuanLiThuVien.Views.UserControls.ThuThu.UcQuanLyPhieuPhat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="1220">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#F4F8FD" BorderBrush="#D5DFEA" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Quản lý Phiếu phạt &amp; Đền bù" FontWeight="Bold" FontSize="18" Foreground="#0B2545" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DataGrid x:Name="DgPhieuPhat" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="#FCFDFF" BorderThickness="0" GridLinesVisibility="Horizontal" RowHeaderWidth="0" Margin="0,0,0,10">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#E8F0FA" />
                        <Setter Property="Foreground" Value="#0B2545" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Setter Property="Height" Value="38" />
                        <Setter Property="Padding" Value="10,0" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Phiếu Phạt" Binding="{Binding MaPhieu}" Width="*" />
                    <DataGridTextColumn Header="Độc Giả" Binding="{Binding DocGia}" Width="2*" />
                    <DataGridTextColumn Header="Sách Vi Phạm" Binding="{Binding SachViPham}" Width="2*" />
                    <DataGridTextColumn Header="Lý Do" Binding="{Binding LyDo}" Width="2*" />
                    <DataGridTextColumn Header="Số Tiền Phạt" Binding="{Binding TienPhat}" Width="*" />
                    <DataGridTextColumn Header="Tình Trạng" Binding="{Binding TinhTrang}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Thêm mới" Width="100" Height="34" Margin="0,0,8,0" Background="#134074" Foreground="White" FontWeight="Bold" BorderThickness="0" />
                <Button Content="Cập nhật" Width="90" Height="34" Margin="0,0,8,0" Background="#1E4D8C" Foreground="White" BorderThickness="0" />
                <Button Content="Làm mới" Width="100" Height="34" Margin="0,0,8,0" Background="#5E728B" Foreground="White" BorderThickness="0" />
                <Button Content="Xóa" Width="90" Height="34" Background="#B42318" Foreground="White" FontWeight="Bold" BorderThickness="0" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
