<UserControl x:Class="DA_QuanLiThuVien.Views.UserControls.ThuThu.UcXyLyYeuCau"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="1220">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#F4F8FD" BorderBrush="#D5DFEA" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Xử lý Yêu cầu từ Độc giả" FontWeight="Bold" FontSize="18" Foreground="#0B2545" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1">
            <TabControl BorderThickness="1" BorderBrush="#D5DFEA" Background="#FCFDFF">
                <TabItem Header="Yêu Cầu Mượn Sách Online" FontSize="14" FontWeight="SemiBold">
                    <Grid Margin="10">
                        <DataGrid x:Name="DgYeuCauMuon" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="#FCFDFF" BorderThickness="0" GridLinesVisibility="Horizontal" RowHeaderWidth="0">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#E8F0FA" />
                                    <Setter Property="Foreground" Value="#0B2545" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Height" Value="38" />
                                    <Setter Property="Padding" Value="10,0" />
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã Yêu Cầu" Binding="{Binding MaYeuCau}" Width="*" />
                                <DataGridTextColumn Header="Độc Giả" Binding="{Binding DocGia}" Width="2*" />
                                <DataGridTextColumn Header="Sách Yêu Cầu" Binding="{Binding SachYeuCau}" Width="2*" />
                                <DataGridTextColumn Header="Ngày Tạo" Binding="{Binding NgayTao}" Width="*" />
                                <DataGridTextColumn Header="Trạng Thái" Binding="{Binding TrangThai}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>
                <TabItem Header="Góp Ý / Yêu Cầu Mua Sách" FontSize="14" FontWeight="SemiBold">
                    <Grid Margin="10">
                        <DataGrid x:Name="DgGopY" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="#FCFDFF" BorderThickness="0" GridLinesVisibility="Horizontal" RowHeaderWidth="0">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#E8F0FA" />
                                    <Setter Property="Foreground" Value="#0B2545" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Height" Value="38" />
                                    <Setter Property="Padding" Value="10,0" />
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã Góp Ý" Binding="{Binding MaGopY}" Width="*" />
                                <DataGridTextColumn Header="Độc Giả" Binding="{Binding DocGia}" Width="2*" />
                                <DataGridTextColumn Header="Nội Dung" Binding="{Binding NoiDung}" Width="4*" />
                                <DataGridTextColumn Header="Ngày Gửi" Binding="{Binding NgayGui}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
