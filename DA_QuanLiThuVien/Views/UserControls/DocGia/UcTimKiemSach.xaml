<UserControl x:Class="DA_QuanLiThuVien.Views.UserControls.DocGia.UcTimKiemSach"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="1220">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F4F8FD" BorderBrush="#D5DFEA" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Margin="0,0,10,0" Height="36" Padding="10,8" VerticalContentAlignment="Center" ToolTip="Nhập tên sách, tác giả, chuyên mục hoặc từ khóa để tìm kiếm" />
                <Button Grid.Column="1" Width="110" Height="36" Content="Tìm kiếm" Background="#134074" Foreground="White" FontWeight="Bold" BorderThickness="0" />
            </Grid>
        </Border>

        <DataGrid Grid.Row="1"
           AutoGenerateColumns="False"
           CanUserAddRows="False"
           IsReadOnly="True"
           GridLinesVisibility="Horizontal"
           HeadersVisibility="Column"
           RowHeaderWidth="0"
           Background="#FCFDFF"
           BorderThickness="1" BorderBrush="#D5DFEA"
           Margin="0,0,0,10">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#E8F0FA" />
                    <Setter Property="Foreground" Value="#0B2545" />
                    <Setter Property="FontWeight" Value="Bold" />
                    <Setter Property="Height" Value="38" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tiêu đề sách" Width="2.5*" />
                <DataGridTextColumn Header="Tác giả" Width="1.5*" />
                <DataGridTextColumn Header="Thể loại" Width="1.5*" />
                <DataGridTextColumn Header="Nhà xuất bản" Width="1.5*" />
                <DataGridTextColumn Header="Năm XB" Width="*" />
                <DataGridTextColumn Header="Trạng thái" Width="1.2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
